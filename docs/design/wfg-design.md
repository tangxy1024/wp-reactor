# WFG 设计文档迁移说明

WFG (`.wfg`) 与 `wfgen` 的 parser、validator、datagen、oracle、verify 和 CLI 实现都归属同级仓库 `warp-fusion`。

主设计文档已迁移到：

`../../../warp-fusion/docs/design/wfg-design.md`

`wp-reactor` 当前不维护 WFG 语言规范；如后续接入 `wfgen`，本目录只保留与 `wp-reactor` 集成相关的说明。
